# 오늘 작업 로그 (2026-05-06)

## 1) 데이터/파일 반영
- `ODV_WIP_20260506.xlsx`를 재고 기준 파일로 반영
- `20260318-M-03 재고 테이블.xlsx` 구조를 ODV 형식으로 교체
- 백업 파일 생성 후 대상 파일 반영 완료

## 2) 대시보드 로직 개선
- 재고 파일 자동 선택 로직 개선
  - `ODV_WIP` 파일 우선 인식
- 재고 컬럼 매핑 개선
  - 고정 인덱스 -> 컬럼명/유연 매핑 방식
- 업데이트 시간 표기 변경
  - 현재시각 -> 데이터 파일 최종 수정시간
  - 시간대 `Asia/Seoul` 기준으로 보정

## 3) 필터/UI 개선
- 이니셜/거래처 드롭다운 -> 검색형으로 변경
- 콤마(`,`) 다중 OR 검색 지원
- 통합 검색창으로 합침 (이니셜/거래처/품목코드/제품명/R코드/Q코드)
- `안전 이니셜 제외` 체크박스 추가
- 숫자 컬럼 천단위 콤마 표시 적용

## 4) 화면 탭 확장
- 상위 탭 추가
  - `제품 부족수량 현황`
  - `리드지 현황`
- `리드지 현황` 내부 탭 추가
  - `리드지 정보`
  - `리드지 재고`

## 5) R/Q 코드 그룹 기능 강화
- `R/Q` 그룹 집계 탭 추가 (`RQ코드 그룹 집계`)
- 필터 추가
  - `RQ 그룹 선택`
  - `동일 RQ그룹만`
- `R코드 제품명` 기준 필터 추가
  - `R코드 제품명 검색`
  - `R코드 제품명 (상위)` pills

## 6) 최종 기준 정리(사용자 요청 반영)
- `R코드/Q코드`는 모두 **파워 포함 형태**로 통일
  - 예: `R1102-03.50`, `Q1102-03.50`
- 표시/필터 기준은 `R코드 제품명` 중심으로 유지
- `RQ코드 그룹 집계`에 `R코드 제품명` 컬럼 표시

## 7) 운영 상태
- Streamlit 서버 재시작 및 HTTP 응답 확인 완료 (`127.0.0.1:8501`)

