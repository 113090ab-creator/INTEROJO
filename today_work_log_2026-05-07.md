# 오늘 작업 로그 (2026-05-07)

## 1) R/Q/P 매핑 및 필터 기준 정리
- R코드(5자리) 기준 P코드(5자리) 종류 2개 이상 조건을 **전역 기본 필터에서 제거**
- 위 조건은 `RQ 그룹 선택` 옵션 생성 시에만 적용되도록 변경
- 기본 적용 문구를 `전체 수요` 기준으로 변경

## 2) 필터/집계 UI 안정화
- `시트 분류`, `분류별 요약`이 전체 수요 기준으로 유지되도록 정리
- `R코드(5자리) 제품명 검색` 위치를 `RQ 그룹 선택` 아래로 유지
- Q/RQ 탭 정렬 시 컬럼 누락으로 발생하던 KeyError 방어 로직 유지/검증

## 3) 보고용 요약(전체 수요 기준) 추가
- `P코드 기준 현황` 탭 상단에 보고용 요약 섹션 추가
- `분류별요약` 기준으로 집계하되, 이니셜 `안전` 포함 여부를 분리 집계
- 사용자 정의 반영:
  - `오더 부족수량 = 안전 미포함`
  - `안전재고 부족수량 = 안전 포함 - 안전 미포함`
  - `총수량 = 오더 부족수량 + 안전재고 부족수량`

## 4) 업데이트 시간 기준 변경
- 화면의 `업데이트` 시간은 `수요정보(전공정)` 파일 수정시간만 사용하도록 변경

## 5) 제품명 기준 정보 재반영 점검
- 수정된 `제품명 기준 정보.xlsx` 파일 인식 확인
- refresh key에 파일 수정시간/크기 반영 확인
- 재집계 검증:
  - 전체 행 수: 1,930
  - `R코드 제품명` 매핑 행: 1,930
  - `R코드5` fallback 행: 0

## 6) 산출 확인(전체 수요)
- 전체 수요 총수량: 17,560,274
- 오더 부족수량: 2,784,129
- 안전재고 부족수량: 14,776,145

---
저장 위치: `C:\Users\유현아\Documents\GitHub\INTEROJO\today_work_log_2026-05-07.md`
