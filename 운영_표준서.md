# 생산 진행 대시보드 운영 표준서

- 문서 버전: v1.5
- 작성일: 2026-05-14
- 최근 개정일: 2026-06-16
- 기준 코드: `app.py`

## 1. 목적
이 문서는 생산 진행 대시보드의 데이터 처리, 매핑, 집계, 화면 표시, 운영 점검 기준을 표준화한다.  
향후 유지보수/배포 시 본 문서를 기준으로 동일한 결과를 재현해야 한다.

## 2. 적용 범위
- Streamlit 대시보드 전체 (`app.py`)
- 대상 화면:
1. 생산 부족 현황
2. 리드지 현황
3. 생산코드별 리드지

## 3. 입력 데이터 표준

### 3.1 파일 종류
1. 재고 파일 (`ODV_WIP*` 우선 탐지)
2. 수요 파일 (`수요정보(전공정)` 우선)
3. 기준 파일 (`제품명 기준 정보`, BOM/분류/리드지 포함)

### 3.2 파일 선택 규칙
1. 재고 파일
- `ODV_WIP` 접두 파일이 있으면 최신 파일 사용
- 없으면 `ODV` + `WIP` 포함명에서 선택
- 그래도 없으면 파일 크기/이름 기반 fallback
2. 수요 파일
- 재고 파일을 제외한 나머지 중 `수요` 관련 파일 선택
- `전공정` 포함명이 있으면 우선
3. 기준 파일
- `제품명` + `기준` 포함 엑셀 중 최신 파일 1개

### 3.3 저장소 파일 관리
1. Excel 임시 잠금 파일은 Git 관리 대상에서 제외한다.
- 제외 패턴: `~$*.xls`, `~$*.xlsx`, `~$*.xlsm`
- 실제 운영 데이터 파일과 임시 잠금 파일을 구분해 커밋한다.
2. 대시보드 로컬 캐시 폴더는 Git 관리 대상에서 제외한다.
- 제외 패턴: `.dashboard_cache/`
- 캐시는 실행 성능 개선용 산출물이므로 원본 데이터나 기준 파일로 취급하지 않는다.

## 4. 업데이트 시간 표준
1. 화면의 `업데이트` 시간은 반드시 `수요정보(전공정).xlsx` 기준으로 표시한다.
2. 시간 산정 우선순위:
- Excel 문서 속성 `modified`
- 속성이 없거나 읽기 실패 시 파일 `mtime`
3. 표시 형식: `YYYY-MM-DD HH:MM:SS` (Asia/Seoul)

## 5. 캐시/성능 표준
1. 캐시 키 분리 원칙
- 데이터 키: 재고/수요/기준 파일 조합 (`build_data_refresh_key`)
- 기준 키: 기준 파일 단독 (`build_reference_refresh_key`)
2. 기준맵 번들 캐시
- BOM/RQ/분류/리드지 맵은 `load_reference_maps_bundle` 캐시 사용
- 수요/재고 파일 변경 시 기준맵 재생성을 최소화
3. 원본 로딩/전처리 분리
- 원본 엑셀 로딩은 `load_raw_data`에서 수행한다.
- 전처리/집계 준비는 `preprocess_data`에서 수행한다.
- 필터 적용은 `filter_data`에서 수행한다.
4. 엑셀 로딩 최소화
- `pd.read_excel()`은 화면 계산에 필요한 컬럼만 `usecols`로 읽는다.
- 수요 파일은 상단 2행을 먼저 읽어 필요한 공정별 컬럼 위치를 계산한 뒤 부분 로딩한다.
- 재고 파일은 품목코드/창고/재고량 컬럼만 부분 로딩한다.
- 리드지정보는 `생산`, `B1코드`, `B1코드명`만 로딩한다.
- 리드지재고는 `품목코드`, `창고`, `재고`만 로딩한다.
- 대용량 엑셀에서 헤더/일부 컬럼/단순 매핑만 필요한 경우 `openpyxl.load_workbook(read_only=True, data_only=True)` 기반 스트리밍 로딩을 우선 검토한다.
- 단, 복잡한 DataFrame 연산이 필요한 구간은 부분 로딩 후 pandas 연산을 사용한다.
5. 메뉴별 지연 로딩
- `리드지 현황`, `생산코드별 리드지` 메뉴 진입 시에만 리드지 기준정보를 로딩한다.
- `리드지 발주현황.xlsx`는 `리드지 현황` 메뉴에서만 로딩한다.
6. 반복 계산 캐시
- 필터 옵션 집계는 `build_filter_option_maps` 캐시를 사용한다.
- 리드지 코드 매핑은 `build_leadji_code_mapping` 캐시를 사용한다.
- 리드지 재고 pivot/groupby는 `build_leadji_stock_pivot` 캐시를 사용한다.
- 화면용 숫자 포맷 변환과 엑셀 다운로드 바이트 생성은 캐시 재사용을 우선한다.
7. 대시보드 디스크 캐시
- 대용량 엑셀 부분 로딩 결과나 BOM 매핑처럼 반복 계산 비용이 큰 결과는 `.dashboard_cache`에 pickle 캐시로 저장할 수 있다.
- 캐시 파일명은 원본 파일명, 파일 크기, 수정시각(`mtime`)과 처리 버전을 포함한 해시 기준으로 생성한다.
- 원본 엑셀 파일이 변경되면 다른 캐시 키를 사용해 이전 캐시가 재사용되지 않도록 한다.
- 캐시 읽기/쓰기 실패는 운영 중단 사유가 아니며, 실패 시 원본 로딩으로 계속 진행한다.
- `.dashboard_cache`는 Git 커밋 대상에 포함하지 않는다.
8. 운영 원칙
- 수요 파일이 자주 바뀌는 운영 패턴에서 응답 지연이 커지면 캐시 키 분리 상태를 최우선 점검한다.
- 별도 parquet/pkl 파일 생성은 기본 데이터 저장 방식으로 사용하지 않는다.
- 예외적으로 성능 개선용 로컬 캐시는 `.dashboard_cache`에 한정해 허용한다.
- 외부 캐시 파일을 도입할 경우 파일명/크기/mtime 기반 무효화 기준을 먼저 정의해야 한다.

## 6. 데이터 처리 파이프라인

### 6.1 수요 데이터
1. 수요 시트는 상단 2행을 먼저 읽어 헤더/공정 정보를 추출한다.
2. 공정별 `생산 수량` 컬럼 위치를 동적으로 추출한 뒤 필요한 컬럼만 부분 로딩한다.
3. 날짜 파싱은 문자열/날짜객체/엑셀 시리얼을 모두 허용한다.
4. 정제 조건:
- 합계행 제외
- 사이트/품목 공백 또는 `nan` 제외
- 품목코드 prefix가 `P/Q/R`이 아닌 행 제외
- `부족수량`과 `사출 생산필요수량`이 모두 0인 행 제외
5. 집계 키: `사이트코드 + 이니셜 + 거래처 + 품목코드`
6. 집계 항목:
- 부족수량 합계
- 사출 생산필요수량 합계
- 리드지 필요수량 합계
- 납기일 최소값
- 사출 납기일 최소값
- 리드지 납기일 최소값
7. 수요정보의 P/R/Q 품목코드 행은 공정별 독립 수요로 취급한다.
- P행은 생산현황 상세 표시의 기준 행이다.
- R/Q행은 같은 수요 맥락의 실제 P행에 연결될 때만 생산현황 P행에 공정 수량으로 반영한다.
- 수요정보에 실제 P행이 없으면 R/Q 수요를 임의의 P코드로 치환하거나 새 P행처럼 표시하지 않는다.
8. R/Q 수요를 P행에 연결할 때는 전체 파워 변형 키를 보존한다.
- 예: `P1121A+00.25+1.50`은 `R1121+00.25+1.50`, `Q1121+00.25+1.50`과 연결해야 하며 `+00.25`만으로 축약 매칭하지 않는다.
- 동일 사이트/이니셜/제품명/파워/납기 또는 수주번호/거래처/이니셜/R코드/Q코드 조합처럼 수요 맥락이 맞는 경우에만 연결한다.

### 6.2 재고 데이터
1. 품목코드/창고/재고 컬럼은 컬럼명 후보 우선 매칭 후 fallback 인덱스 사용
2. 창고명은 내부 표준으로 정규화
- 사출창고
- 분리창고
- 검사접착창고
- 검사접착재작업창고
- 누수규격검사창고
3. 공정재고 합계 산식:
- `공정재고 합계 = 사출창고 + 분리창고 + 검사접착창고 + 검사접착재작업창고 + 누수규격검사창고`

## 7. R/Q 코드 매핑 표준

### 7.1 기본 규칙
1. 기본 추론: 품목코드 기준 `P -> R`, `P -> Q` 변환
2. 비P 코드 보정: 리드지 맵 우선, 없으면 분류정보 R/Q 맵 사용

### 7.2 BOM 적용 우선순위
1. BOM exact (`TO_ITEM_ID` 정확 일치) 최우선
2. 아직 유효하지 않은 R/Q 행에 BOM base fallback
3. 유효성 판단:
- R코드는 `R`로 시작
- Q코드는 `Q`로 시작

### 7.3 재고 매칭 보정
1. 재고 조회 시 `BUL1/BUL2`는 `BUL` 후보까지 확장 조회
2. 실제 재고키가 있는 공정코드 후보를 우선 채택
3. 분리창고는 Q코드 재고가 없을 때 `리드지정보.외주` 기반 U코드 후보를 추가 조회한다.

### 7.4 매핑 충돌 처리 원칙
1. `BOM exact > 유효성 보정(BOM base) > 기본/참조맵`
2. 충돌 시 상위 우선순위 결과를 강제 적용

## 8. 분류/시트분류 표준
1. P코드: `코드5` 직접 매핑
2. 비P코드: 동일 `R코드5`를 갖는 P코드의 분류 상속
3. 결측 기본값:
- 시트분류: `기타_해외`
- 분류요약: `기타`

## 9. 핵심 산식 표준
1. 전체수요 요약(분류요약별):
- 리더 부족수량 = 안전 제외 부족수량
- 안전재고 부족수량 = 안전 포함 부족수량 - 안전 제외 부족수량
- 총수요 = 리더 부족수량 + 안전재고 부족수량
2. RQ 그룹 산식:
- 사출 부족수량 = `max(사출 생산필요수량 합계 - 사출창고 합계, 0)`

## 10. 화면/필터 표준

### 10.1 상위 메뉴
1. 생산 부족 현황
2. 리드지 현황
3. 생산코드별 리드지

### 10.2 생산 부족 현황 하위 메뉴
1. 생산 현황
2. 사출 현황
3. 공용 품목 현황

### 10.3 필터 규칙
1. 사이트코드 pills (수량 합계 표시)
2. 통합검색 (콤마 `,` 기준 OR 검색)
3. 시트분류 pills
4. 분류요약 pills
5. 옵션:
- 공정재고만
- 안전 이니셜 제외
- 동일 RQ그룹만 (`R5/Q5`, `P5` 2종류 이상)
6. 데이터 소스 선택
- 사이드바 데이터 소스 영역은 toggle 기반 UI를 기본으로 한다.
- Streamlit expander 아이콘 폰트가 `_arrow_right` 같은 텍스트로 노출될 수 있으므로 데이터 소스 핵심 UI에는 expander 사용을 지양한다.

### 10.4 표/다운로드
1. 각 현황 탭에 엑셀 다운로드 버튼을 제공한다.
2. 표 컬럼 폭은 글자수 기준 `고정 px 폭`으로 설정한다.
3. 폭 계산 기준:
- 숫자형 컬럼: 더 좁은 폭 우선(요약 수치 가독성 + 화면 수용성)
- 코드형 컬럼(R/Q/P/생산/리드지): 중간 고정폭
- 긴 텍스트 컬럼(제품명/리드지명): 상대적으로 넓은 고정폭
4. 폭 산정 시 헤더 길이와 값 길이의 `p90 길이`를 함께 사용한다.
5. 표 인덱스 컬럼은 기본 숨김(`hide_index=True`)으로 가로 공간을 확보한다.
6. 긴 헤더는 축약 라벨을 사용해 폭을 절감한다.
- 예: `누수규격검사 창고 -> 누수규격`, `검사접착창고 -> 검사접착`, `공정재고 합계 -> 공정재고`
7. 숫자 표시는 천단위 구분기호 형식을 적용한다.
8. 운영 화면은 `layout="wide"`와 `.block-container max-width: 100%`를 기본으로 사용한다.
9. 생산현황 표에서는 `상태` 컬럼을 표시하지 않는다.
10. 수량/부족/재고/창고/발주/합계 계열 숫자는 화면 표시 시 정수 반올림으로 표시한다.
11. 부족 컬럼 스타일은 각 컬럼의 자기 값 기준으로 독립 적용한다.
12. 생산 부족 현황 하단 상세 표에서는 운영 판단에 불필요한 내부/중간 산출 컬럼을 표시하지 않는다.
- 미표시 컬럼: `상태`, `수주번호`, `수요`, `분리필요`, `45필요`, `55필요`
- 내부 매핑이나 필터에 필요한 원본 컬럼은 처리 로직 안에서만 유지할 수 있다.
13. 생산 부족 현황 직접 검색은 현재 화면에 표시되는 하단 데이터 표의 모든 컬럼을 대상으로 한다.
- 고정 컬럼 목록으로 검색하지 않는다.
- 화면에서 제거한 컬럼은 직접 검색 대상에서도 제외한다.
14. 대용량 표는 화면에서 전체 필터 결과를 표시한다.
- 별도 표시 건수 선택 UI를 두지 않는다.
- 다운로드도 현재 직접 검색까지 반영된 표시 결과 기준으로 제공한다.
15. 운영 화면에서는 진단용/중복 요약 패널을 기본 표시하지 않는다.
- 미표시 패널: `전체 수요 요약`, `R/Q 독립 수요 확인`, `미분류 제품 목록`

## 11. 리드지 현황 표준
1. 기준 시트:
- 리드지정보
- 리드지재고
2. 기준 데이터 로딩 범위:
- 리드지정보는 `생산`, `B1코드`, `B1코드명` 컬럼만 사용한다.
- 리드지재고는 `품목코드`, `창고`, `재고` 컬럼만 사용한다.
- 리드지 코드 매핑은 `build_leadji_code_mapping`에서 생성/캐시한다.
- 리드지 창고별 재고 집계는 `build_leadji_stock_pivot`에서 생성/캐시한다.
3. 집계 기준:
- 대상은 P코드 부족수요
- `P코드5 -> B1코드` 매핑 후 리드지 코드 단위 집계
4. 지표:
- 생산필요수량 합계
- 최소납기일
- 창고별 재고 (합계가 0인 창고는 미표시)
5. 리드지 발주/구매의뢰 연동 기준:
- `리드지 발주현황.xlsx`를 기준 파일로 사용한다.
- `구매발주현황` 시트:
  - J열 품목코드
  - O열 미입고수량
  - X열 납기일자
- `구매의뢰현황` 시트:
  - G열 품목코드
  - U열 발주잔량
  - Y열 요청일
- `발주수량 = 구매발주수량 + 구매의뢰수량`으로 표시한다.
- 입고예상일자는 구매발주/구매의뢰 기준 일자 중 가장 빠른 일자를 사용한다.
- 구매의뢰 기준 입고예상일자는 확정 입고일이 아니므로 화면에 구매팀 확인 필요 문구를 표시한다.
6. 리드지 화면 표시 기준:
- 기본 목록에는 `상태` 컬럼을 표시하지 않는다.
- `발주 반영 품목 리스트`는 `발주수량 > 0`인 리드지만 표시한다.
- `발주 반영 품목 리스트`에는 `구매발주수량`, `구매의뢰수량`을 함께 표시한다.
- 발주/의뢰 데이터만 있고 리드지정보 B1코드가 없는 품목은 `매칭필요:{P코드}` 형태로 표시한다.
- 창고별 상세 재고는 기본 목록에서 분리해 expander로 제공한다.

## 12. 예외/오류 처리 표준
1. 필수 파일 부족 시 로딩 실패 메시지로 중단한다.
2. 주요 컬럼 미탐지 시 fallback 컬럼 후보를 순차 적용한다.
3. 날짜 파싱 실패 값은 `-`로 표시한다.
4. 재고/매핑 결측은 0 또는 기본 텍스트로 안전 대체한다.

## 13. 변경관리 표준 (운영 체크리스트)
배포 전 아래 항목을 반드시 확인한다.

1. 입력 파일 확인
- 수요/재고/기준 파일이 최신인지
- 파일명 규칙이 탐지 규칙과 충돌하지 않는지
- Excel 임시 잠금 파일(`~$*.xlsx` 등)이 커밋 대상에 포함되지 않았는지
- `.dashboard_cache/`가 커밋 대상에 포함되지 않았는지
2. 매핑 확인
- 샘플 코드 5건 이상에서 R/Q 결과가 기대값과 일치하는지
- BOM exact 우선 적용 케이스가 정상인지
- 다중 파워 코드가 전체 파워 키 기준으로 연결되는지
- 수요정보에 실제 P행이 없는 R/Q 수요가 임의 P행으로 치환되지 않는지
3. 산식 확인
- 공정재고 합계/사출 부족수량/RQ 그룹 집계가 수동 계산과 일치하는지
4. 화면 확인
- 업데이트 시간이 수요정보(전공정) 기준인지
- 각 탭 다운로드 버튼 정상 동작 여부
- 생산 부족 현황 하단 표에서 미표시 컬럼(`수주번호`, `수요`, `분리필요`, `45필요`, `55필요`)이 노출되지 않는지
- 직접 검색이 현재 표시 표의 모든 컬럼 기준으로 동작하는지
- 표시 건수 선택 UI 없이 전체 결과가 표시되는지
5. 성능 확인
- 첫 로딩, 재로딩, 수요 파일 교체 후 재로딩 시간을 기록
- 기준맵 캐시 재사용 여부 확인
- 원본 엑셀 교체 후 이전 `.dashboard_cache`가 잘못 재사용되지 않는지 확인

## 14. Streamlit Cloud 자동 유지 표준
1. 목적
- 무료 Streamlit Cloud 앱의 장시간 미접속 Sleep 진입 가능성을 낮추기 위해 GitHub Actions keepalive 워크플로우를 사용한다.
2. 워크플로우 파일
- 경로: `.github/workflows/keepalive.yml`
- 이름: `Keep Streamlit Alive`
- 실행 방식: `workflow_dispatch` 수동 실행 및 cron 주기 실행
- 기본 주기: 8시간마다 실행 (`0 0,8,16 * * *`)
3. Secret
- Repository secret 이름은 `STREAMLIT_URL`로 고정한다.
- 값은 실제 Streamlit 앱 주소 전체 URL을 입력한다.
- 예: `https://interojo-2.streamlit.app/`
4. 운영 기준
- 최초 설정 후 Actions에서 수동 실행해 Success 여부를 확인한다.
- 8시간 주기로도 Sleep이 반복되면 6시간, 4시간 순서로 주기를 단축한다.
- 이 방식은 예방용이며 이미 Sleep 상태가 된 앱의 복구 버튼 클릭까지 자동화하지는 않는다.

## 15. 표준 변경 이력
1. v1.5 (2026-06-09)
- 수요정보 P/R/Q 행을 공정별 독립 수요로 취급하는 기준 추가
- R/Q 연결 시 전체 파워 변형 키를 보존하는 기준 추가
- 실제 P행이 없는 R/Q 수요를 임의 P코드로 치환하지 않는 기준 추가
- 생산 부족 현황 하위 메뉴에서 `분리 현황` 제거
- 하단 상세 표 미표시 컬럼 기준 추가 (`수주번호`, `수요`, `분리필요`, `45필요`, `55필요`)
- 직접 검색 대상을 현재 표시 표의 모든 컬럼으로 변경
- 대용량 표 전체 표시 및 표시 건수 선택 UI 제거 기준 추가
- 진단용/중복 요약 패널 기본 미표시 기준 추가

2. v1.4 (2026-06-08)
- 대용량 엑셀 스트리밍 로딩 기준 추가 (`openpyxl read_only=True`)
- `.dashboard_cache` 기반 로컬 디스크 캐시 허용 기준 추가
- 캐시 무효화 기준을 원본 파일명/크기/mtime/처리 버전 기반으로 명시
- 캐시 읽기/쓰기 실패 시 원본 로딩으로 계속 진행하는 운영 원칙 추가
- `.dashboard_cache/` Git 제외 및 커밋 전 확인 항목 추가

3. v1.3 (2026-06-04)
- Streamlit 성능 최적화 기준 추가 (`load_raw_data`, `preprocess_data`, `filter_data` 분리)
- 엑셀 `usecols` 부분 로딩과 메뉴별 지연 로딩 원칙 추가
- 필터 옵션/리드지 매핑/리드지 재고 pivot 반복 계산 캐시 기준 추가
- 대용량 표 화면 표시 제한 기준 추가 (`DATAFRAME_DISPLAY_ROW_LIMIT = 1000`)
- 사이드바 데이터 소스 toggle UI 기준 추가
- Streamlit Cloud keepalive GitHub Actions 운영 기준 추가
- Excel 임시 잠금 파일 `.gitignore` 제외 기준 추가

4. v1.2 (2026-06-02)
- 화면명을 `생산현황` 운영 대시보드 기준으로 정리
- 상위 메뉴를 `생산 부족 현황`, `리드지 현황`, `생산코드별 리드지`로 표준화
- 생산현황 표의 `상태` 컬럼 미표시 원칙 추가
- 수량/부족/재고/창고/발주/합계 계열 정수 표시 원칙 추가
- 생산 부족 현황 직접 검색 기준 추가
- 리드지 발주/구매의뢰 연동 기준 추가
- 리드지 `발주 반영 품목 리스트` 기준 추가

5. v1.1 (2026-05-15)
- 컬럼 간격 정책을 `자동폭`에서 `글자수 기반 고정폭(px)`으로 변경
- 표 인덱스 숨김(`hide_index=True`)을 기본값으로 적용
- 주요 긴 헤더 라벨 축약 규칙 추가

6. v1.0 (2026-05-14)
- 업데이트 시간 기준을 `수요정보(전공정)`으로 고정
- BOM 기반 R/Q 보정 우선순위 반영
- 탭별 엑셀 다운로드 버튼 활성화
- 컬럼 자동 폭 적용
- 참조 캐시 분리로 성능 개선 구조 확정
