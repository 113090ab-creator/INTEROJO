# 오늘 작업 로그 (2026-06-02)

## 1) 리드지 발주/구매의뢰 데이터 반영
- `리드지 발주현황.xlsx` 분석 및 연동 기준 반영
- `구매발주현황` 시트 기준
  - J열: 품목코드
  - O열: 미입고수량
  - X열: 납기일자
- `구매의뢰현황` 시트 기준
  - G열: 품목코드
  - U열: 발주잔량
  - Y열: 요청일
- `발주수량 = 구매발주수량 + 구매의뢰수량`으로 리드지 현황에 합산 반영
- 구매발주에는 없고 구매의뢰에만 있는 건도 누락되지 않도록 반영
- 발주/의뢰만 존재하고 리드지정보 B1코드가 없는 품목은 `매칭필요:{P코드}` 형태로 표시

## 2) 리드지 현황 운영 화면 개선
- 상단 안내 문구를 업무용 문장으로 변경
  - `입고예정일자는 구매의뢰 기준입니다. 실제 입고 일정은 구매팀 확인이 필요합니다.`
- 리드지 KPI를 4개 카드로 재구성
  - 전체 리드지코드
  - 부족 리드지
  - 입고예정
  - 총 리드지 부족수량
- `재고합계` 컬럼 추가
- 창고별 상세 재고는 expander로 분리
- `우선 확인 필요 리스트`를 제거하고 `발주 반영 품목 리스트`로 변경
  - `발주수량 > 0`인 리드지만 표시
  - `구매발주수량`, `구매의뢰수량`을 함께 표시
  - 입고예상일자 빠른 순, 부족수량 큰 순으로 정렬
- 리드지 현황 기본 목록 및 다운로드에서 `상태` 컬럼 제거

## 3) 생산현황 대시보드 UI/UX 개선
- 시스템명과 화면 타이틀을 `생산현황`으로 변경
- 상단 설명 문구 변경
  - 생산 부족 리스크, 공정 재고, 자재 입고 현황을 실시간으로 모니터링하는 화면으로 정리
- 메뉴명 변경
  - `제품 부족수량 현황` -> `생산 부족 현황`
  - `생산코드 기준 리드지` -> `생산코드별 리드지`
- 공정별 메뉴명 변경
  - `사출 생산 현황` -> `사출 현황`
  - `분리 생산 현황` -> `분리 현황`
  - `사출, 분리 공용 품목 생산 현황` -> `공용 품목 현황`
- `st.set_page_config(page_title="생산현황", layout="wide")` 적용
- `.block-container` 최대 폭 제한 제거
  - `max-width: 100%`
  - 좌우 padding `2rem`
- 밝은 운영 대시보드 스타일 적용
  - 배경: `#FFFFFF`
  - 서브 배경: `#F4F6F8`
  - 포인트 컬러: `#1A2B5E`
  - 폰트: Inter

## 4) KPI/테이블 가독성 개선
- 생산현황 KPI를 전용 HTML 카드로 변경해 긴 숫자 잘림 방지
- 위험 KPI는 빨간색 계열, 재고 KPI는 네이비/파란색 계열로 구분
- KPI 라벨 축약
  - 사출창고 합계 -> 사출 재고
  - 분리창고 합계 -> 분리 재고
  - 검사접착창고 합계 -> 검사접착 재고
  - 누수규격검사창고 합계 -> 누수규격 재고
- 제품명/코드/숫자 컬럼 폭 정책 보정
- 생산현황, 사출 현황, 분리 현황, 공용 품목 현황에서 `상태` 컬럼 제거
- 숫자 표시 정책 보정
  - `수량`, `부족`, `재고`, `창고`, `발주`, `합계` 계열은 화면에서 정수 반올림 표시
  - `사출부족`의 `.00` 표시 제거
- 부족수량 색상 버그 수정
  - 각 부족 컬럼이 자기 값 기준으로 빨간색/bold 처리되도록 람다 클로저 문제 수정

## 5) 검색 기능 개선
- 생산 부족 현황 화면에 직접 검색창 추가
- 검색 대상
  - 거래처
  - 이니셜
  - 품목코드
  - R코드
  - Q코드
  - 제품명
  - R코드 제품명
- 콤마(`,`) 입력 시 OR 검색 적용
- 검색 결과 기준으로 KPI와 테이블이 함께 갱신되도록 연결

## 6) 검증
- 문법 검증: `python -m py_compile app.py` 통과
- Streamlit 응답 확인
  - `http://127.0.0.1:8501`
  - HTTP 200 확인
- 숫자 표시 샘플 검증
  - `282000.00` -> `282,000`
  - `248664.00` -> `248,664`

---
저장 위치: `C:\Users\유현아\Documents\GitHub\INTEROJO\today_work_log_2026-06-02.md`
