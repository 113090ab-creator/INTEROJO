# 오늘 작업 로그 (2026-05-15)

## 1) 리드지 현황 엑셀 다운로드 기능 추가
- `리드지 현황` 화면에 `엑셀 다운로드` 버튼 추가
- 현재 화면 필터 결과(`filtered_summary`)를 그대로 내려받도록 적용
- 파일명 규칙: `leadji_status_YYYYMMDD_HHMMSS.xlsx`

## 2) 신규 화면 추가: 생산코드 기준 리드지 현황
- `리드지 현황` 옆에 신규 화면 추가
  - 초기 요구사항인 `P코드5 기준`을 구현 후, 사용자 요청에 따라 화면 표기를 `생산코드`로 변경
- 제공 컬럼
  - `생산코드`, `리드지코드`, `리드지명`, `생산필요수량`, 창고별 재고 컬럼, `최소납기일`
- 기능
  - 통합 검색(생산코드/리드지코드/리드지명/창고)
  - 지표(생산코드 수, 생산코드 기준 생산필요수량 합계, 최소납기일)
  - 엑셀 다운로드
    - 파일명: `leadji_status_by_production_code_YYYYMMDD_HHMMSS.xlsx`

## 3) 집계 정확도 보정
- 생산코드 1개에 리드지 다중 매핑이 가능한 구조를 고려해,
  지표의 생산필요수량 합계는 `생산코드 중복 제거` 기준으로 계산되도록 보정
- 화면에 다중 매핑 시 수량이 행별 반복 표시될 수 있다는 기준 문구 추가

## 4) 대시보드 성능 최적화
- 상단 메뉴와 부족수량 상세 메뉴를 `탭(st.tabs)` 기반에서 `선택형(st.segmented_control)` 기반으로 변경
  - 선택된 화면만 계산/렌더링되도록 구조 전환
  - 기존 `st.tabs` 특성(모든 탭 계산)을 회피하도록 변경
- 부족수량 화면에서 선계산되던 요약(`q_summary`, `rq_summary` 등)을
  선택된 화면에서만 계산하도록 조정
- 캐시 강화
  - 아래 캐시 함수에 `persist="disk"` 적용
    - `load_reference_maps_bundle`
    - `load_data`
    - `load_leadji_data`

## 5) 안정성/검증
- 문법 검증: `python -m py_compile app.py` 통과
- 스모크 확인
  - 데이터 로드 후 행 수/시트 로딩 정상 확인
  - 신규 화면 라벨/검색/다운로드 동작 경로 코드 반영 확인

---
저장 위치: `C:\Users\유현아\Documents\GitHub\INTEROJO\today_work_log_2026-05-15.md`
