# 오늘 작업 로그 (2026-06-09)

## 1) 작업 범위
- 기준 브랜치: `main`
- 작업 커밋 범위: `53fe34f` ~ `0108165`
- 최종 앱 코드 기준 커밋: `0108165 Search visible dashboard columns`
- 배포 앱: `https://interojo-2.streamlit.app/`

## 2) 작업 파일
- `app.py`
- `운영_표준서.md`
- `today_work_log_2026-06-09.md`
- `ODV_WIP_20260506.xlsx`
- `수요정보(전공정).xlsx`
- `제품명 기준 정보.xlsx`
- `리드지 발주현황.xlsx`

## 3) 수요정보/RQ 매핑 검토 및 보정
- 수요정보 구조를 P/R/Q 품목코드 행이 공정별로 분리된 독립 수요 구조로 재정의
- R/Q 공정 수량을 생산현황 P행에 연결할 때 전체 수요 범위를 기준으로 연결하도록 보정
- 사이트/거래처/이니셜/R코드/Q코드 맥락이 맞지 않는 수요가 섞이지 않도록 연결 키를 강화
- `P1121A+00.25+1.50`처럼 파워가 2개 이상 포함된 코드는 전체 파워 변형 키로 매칭하도록 수정
- 수요정보에 실제 P행이 없는 R/Q 수요는 임의 P코드로 치환하지 않도록 기준을 강화

## 4) 누락 수요 재발 방지 조치
- R/Q 연결 검증 지표를 보강해 P행 반영 수량과 미매핑 R/Q 수량을 구분
- 실제 P행이 있는 경우에만 R/Q 수량이 생산현황에 반영되도록 `build_synthetic_p_rows_for_process_scope()` 흐름을 조정
- R/Q 매핑에서 짧은 파워값만 보고 다른 제품을 연결하는 문제를 방지
- `APP_CACHE_VERSION`을 `20260609-process-coverage-v20`까지 갱신해 Streamlit 캐시가 새 처리 기준을 사용하도록 변경

## 5) 화면 표시 기준 변경
- 생산 부족 현황 하위 메뉴에서 `분리 현황` 탭 삭제
- `미분류 제품 목록` 패널 삭제
- `전체 수요 요약`, `R/Q 독립 수요 확인` 등 진단용 요약 패널 삭제
- 상세 표에서 다음 컬럼을 삭제
  - `수요`
  - `분리필요`
  - `45필요`
  - `55필요`
  - `수주번호`
- 생산현황 KPI/요약에서 분리필요수량, 55필요수량 등 중간 공정 필요수량 카드를 제거

## 6) 검색/표시 건수 기준 변경
- 표시 건수 선택 UI를 삭제
- 하단 데이터 표는 필터 결과 전체를 표시하도록 변경
- 직접 검색은 고정 컬럼 목록이 아니라 현재 하단 표에 표시되는 모든 컬럼을 대상으로 검색하도록 변경
- 화면에서 제거한 컬럼은 직접 검색 대상에서도 제외되도록 변경
- 직접 검색 결과가 엑셀 다운로드에도 반영되도록 표시 표 기준 데이터를 다운로드 대상으로 사용

## 7) 성능/운영 안정화
- 대시보드 렌더링 및 기준정보 캐시 경로를 정리
- 대용량 표 제한을 제거하되 기존 `limit_dataframe_for_display()` 흐름은 no-op으로 유지해 호출부 영향 최소화
- Streamlit Cloud 반영 지연 시 `Manage app`에서 reboot/log 확인이 필요함을 확인
- 앱 사이드바 버전 표시로 실제 배포 코드 기준을 확인할 수 있도록 유지

## 8) 표준서 반영
- `운영_표준서.md` 문서 버전을 `v1.5`로 갱신
- 최근 개정일을 `2026-06-09`로 변경
- 수요정보 P/R/Q 독립 수요 기준 추가
- 전체 파워 키 기반 R/Q 연결 기준 추가
- 실제 P행 없는 임의 P치환 금지 기준 추가
- 생산 부족 현황 메뉴/표시 컬럼/직접검색/전체 표시 기준을 오늘 화면 기준으로 갱신
- 변경관리 체크리스트에 다중 파워 매핑, 임의 P치환 방지, 미표시 컬럼, 직접검색 전체 컬럼 기준 확인 항목 추가

## 9) 검증 상태
- `python -m compileall app.py` 통과
- `git diff --check` 통과
- 앱 코드 변경은 `main`에 푸시 완료
- 문서 변경은 본 로그와 표준서 갱신 커밋으로 반영

## 10) 현재 기준 상태
- 최신 앱 코드 커밋: `0108165 Search visible dashboard columns`
- 앱 캐시 버전: `20260609-process-coverage-v20`
- 운영 표준서 버전: `v1.5`
- 기준 배포 URL: `https://interojo-2.streamlit.app/`

---
저장 위치: `C:\Users\유현아\Documents\GitHub\INTEROJO\today_work_log_2026-06-09.md`
